import React from "react";
import SupportRelatedPage from "./SupportRelatedPage";

export default function page() {
  return (
    <div className="top-padding">
      <SupportRelatedPage />
    </div>
  );
}
